import React from 'react';
import './index.scss';

const NavBar = () => {
  const bloup = 'NavBar here!';

  return (
    <div className="NavBar">
      <p>{bloup}</p>
    </div>
  );
};

export default NavBar;
